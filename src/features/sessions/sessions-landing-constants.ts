/**
 * Default `limit` for the Sessions landing `interactionSessions` query so each page
 * shows a fixed row count and the table area does not jump with varying result sizes.
 */
export const SESSIONS_LANDING_PAGE_SIZE = 5
