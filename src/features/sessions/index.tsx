import { SessionsView } from '@/features/sessions/sessions-view'

/** Landing page: dashboard GraphQL explorer + sessions table + session detail sidebar. */
export function Sessions() {
  return <SessionsView />
}
