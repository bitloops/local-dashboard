import type { ResolvedTheme } from '@/context/theme-provider'

/**
 * Resolved UI theme for query-explorer (`ThemeProvider`’s `resolvedTheme` after system resolution).
 */
export type QueryExplorerResolvedTheme = ResolvedTheme
